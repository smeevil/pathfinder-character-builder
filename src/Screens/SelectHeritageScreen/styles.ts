import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cardContainer: {
    flex: 1,
    paddingTop: 20,
  },

  container: {
    flex: 1,
    padding: 10,
  },

  layout: {
    alignContent: 'center',
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },

  select: {
    width: '100%',
  },
})
